#include "../inc/pawn_logic.h"


std::vector<Move> PawnLogic::getAvaiableMoves(int startRow, int startColumn, const std::vector<std::vector<Square>>& board) const {
    auto avaiableMoves = std::vector<Move>{};
    

}